# Introduce ~nixosSharedModules~ flake attribute, with the type ~listOf deferredModule~

# How dendritic module looks like

```nix
{self, inputs, lib, ...}: {flak
}

```

homeModules.default
nixosModules.default

  #+begin_src nix

  #+end_src
