#+TITLE: nct (nixos-config-tool)

* Goal

Scaffold a new Rust-based CLI tool =nct= (executable =nct=) to eventually
assist with NixOS configuration management in this repo. Companion / eventual
successor-ish to the Python-based =ncf= tool, but starting from a minimal
scaffold.

* Stage 0 — scaffold [DONE]

** What was created
- =tools/nct/= :: Rust binary crate (edition 2024).
  - =Cargo.toml= :: deps: =clap= (derive), =anyhow=, =tokio= (full);
    dev-dep =assert_cmd=.
  - =Cargo.lock= :: generated + committed (required by =buildRustPackage=).
  - =src/main.rs= :: clap-derive =Cli= with a =nct machine provision <name>=
    stub + 2 unit tests (parse + missing-subcommand rejection).
  - =.gitignore= (=target=), =.envrc= (=use flake ../../#nct=), =README.md=.
- =modules/nct.nix= :: flake-parts module (dendritic pattern, auto-discovered
  by =import-tree=):
  - =packages.nct= :: =rustPlatform.buildRustPackage=, =meta.mainProgram =
    "nct"=.
  - =devShells.nct= :: cargo / rustc / rust-analyzer / clippy / rustfmt.
  - =overlays.nct=.
- =modules/devshell.nix= :: added =self'.packages.nct= to =devShells.default=.
- =modules/formatter.nix= :: enabled =programs.rustfmt= in treefmt.
- =.forgejo/workflows/tool-tests.yaml= :: added an =nct-tests= job there
  (mirroring the existing =ncf-tests= / =monitoring-tests= conventions:
  =runs-on: native=, =env-aware-nix-develop=, master + PR triggers) running
  =cargo test= in =.#devShells.x86_64-linux.nct= and =nix build .#nct=.

** Decisions
- Async-first: =tokio= (full) pulled in from the start; =main= is
  =#[tokio::main]=.
- =nct= lives in the default devshell (like =ncf=) so it's on PATH in the main
  shell, in addition to its dedicated =.#devShells.x86_64-linux.nct=.
- No new flake inputs were added, so =nix run .#write-flake= was **not**
  needed (=flake.nix= untouched).
- Used =selfLib.dir'= / =file'= for src + cargoLock (avoids flake-source
  leakage, consistent with =niri-dynamic-keybindings= / =waybar-org-clock=).

** Verification [DONE]
- [X] =nix build .#nct= builds; =cargo test= runs during check (2/2 pass).
- [X] =./result/bin/nct --version= = =nct 0.1.0=;
      =nct machine provision test-host= works.
- [X] =cargo test= in =.#devShells.x86_64-linux.nct= = 2 passed.
- [X] =nct= resolves via =command -v nct= in =.#devShells.x86_64-linux.default=.
- [X] =nix fmt= runs rustfmt over =.rs= files.

* Stage 1+ — TODO

** =machine provision= implementation
Currently just prints the machine name. Needs real logic. Likely overlaps with
=ncf='s =add_machine= / =init_machine= / =provision_vm= commands — decide
whether =nct= ports those or wraps =ncf=.

** Subcommand surface
Decide the command tree. Candidate stubs mirroring =ncf=:
- =nct machine {add,init,provision,list,...}=
- =nct build=, =nct eval=, =nct deploy=
- =nct secret ...= (sops), =nct ts ...= (tailscale), =nct ci ...=

** Shared infra with =ncf=
- Determine whether =nct= reuses =ncf= runtime deps (git-crypt, sops,
  ssh-to-age, deploy-rs, ...) by wrapping them at the nix level
  (=postFixup wrapProgram=) like =ncf= does, or shells out at runtime.
- Decide on nix invocation strategy (eval/build via =Command= calls vs. an
  in-process nix library). → *resolved in Stage 2: in-process via
  nix-bindings-rust.*

** Quality
- [X] Add clippy to CI (not a treefmt program; run via devshell).
      Added a =cargo clippy --all-targets -- -D warnings= step to the
      =nct-tests= job in =.forgejo/workflows/tool-tests.yaml=.
- [ ] Consider =deny(warnings)= once surface stabilizes.
- [~] Integration tests via =assert_cmd= (already a dev-dep) exercising the CLI
      end-to-end. → *partially superseded*: Stage 2 added =#[ignore]d=
      in-process integration tests (=eval_flake_apply= against a fixture
      flake) instead of spawning the =nct= binary via =assert_cmd=. CLI arg
      parsing is covered by unit tests. A true end-to-end =assert_cmd= test
      (e.g. =nct machine eval-expr --help=) is still a reasonable follow-up.

* Stage 2 — nix-bindings-rust integration + =machine eval-expr=

** Context / investigation

Upstream: [[https://github.com/nixops4/nix-bindings-rust][nixops4/nix-bindings-rust]], checked out locally at
=../nix/nix-bindings-rust/= (reference only).

*** Crate layout (workspace)
- =nix-bindings-util= — version detection (parses =pkg-config --modversion
  nix-store-c=, emits =cfg(nix_at_least="2.26")= etc.), settings, context.
- =nix-bindings-store= — =Store::open=, =StorePath=.
- =nix-bindings-expr= — =EvalState= / =EvalStateBuilder=, =eval_from_string=,
  =require_*= (int/bool/string/list/attrs), =call=, =call_multi=,
  =gc_register_my_thread=, =init=.
- =nix-bindings-flake= — =FlakeSettings= + =EvalStateBuilderExt::flakes=,
  enables =builtins.getFlake= / flake refs in an =EvalState=. Depends on
  =nix-bindings-fetchers= (which needs Nix ≥ 2.29).
- =nix-bindings-fetchers= — =FetchersSettings=; Nix ≥ 2.29.
- =-sys= siblings: bindgen-generated FFI, not for direct use.

*** Build requirements (the hard part)
These are not pure-Rust crates. =build.rs= / =pkg-config= / bindgen need, at
build time:
- Nix's C headers + libraries, discoverable as =nix-store-c= / =nix-expr-c=
  via pkg-config. The version string drives =cfg(nix_at_least=...)=.
- =pkg-config= (nativeBuildInput).
- =libclang= / =clang-unwrapped= for bindgen (=LIBCLANG_PATH=,
  =BINDGEN_EXTRA_CLANG_ARGS=-x c++ -std=c++2a=).
- =stdenv.cc= (for =stdbool.h=).
- With gcc stdenv: the upstream =bindgen-gcc.sh= shim sets
  =NIX_CC_UNWRAPPED= so bindgen finds system headers.

Upstream provides an =nciBuildConfig= flake-parts module for nci users; we use
=rustPlatform.buildRustPackage= (see =modules/nct.nix=), so we must replicate
that config manually as =nativeBuildInputs= / =buildInputs= / env on the
derivation. Reference impl: =../nix/nix-bindings-rust/flake.nix= →
=config.nix-bindings-rust.nciBuildConfig=.

Local Nix version: =2.34.8= (Determinate), so the ≥ 2.29 fetchers floor and
≥ 2.26 =EvalStateBuilder= are both satisfied.

*** Pull strategy (decided)
- **No flake input.** Pull the crates via Cargo, not via
  =inputs.nix-bindings-rust=.
- Initial: git source in =Cargo.toml=
  (=git = "https://github.com/nixops4/nix-bindings-rust"=), so it builds in CI
  and on other machines. Regenerate =Cargo.lock=.
- Optional dev convenience later: a =.cargo/config.toml= =path= override
  pointing at =../nix/nix-bindings-rust= for local hacking (kept out of the
  committed build path).

** Playground target

#+begin_src shell
nct machine eval-expr llm-runner 'm: builtins.attrNames m.config.llama-models.configurations'
#+end_src

Semantics (mirrors =nix eval --apply=):
1. Load this flake's =nixosConfigurations.<name>= (full NixOS module eval) as a
   Nix value, via =builtins.getFlake=. <name> is =llm-runner=.
2. Apply the user lambda (=m: ...=) to that value (so =m= is the whole
   =nixosConfigurations.llm-runner= attrset — /note/: this is the *top-level*
   machine attrset, i.e. ={ config, pkgs, ... }= result, so =m.config.X= is the
   right access pattern).
3. Print the result: strings verbatim, lists/attrsets as a JSON-ish dump
   (reuse =nix eval='s output shape where cheap; full pretty-print is a
   follow-up).

A flake ref is needed because the playground reads =.#nixosConfigurations=;
so =EvalState= must be built with flakes enabled
(=EvalStateBuilder::flakes(&flake_settings)=).

** Tasks [DONE]

*** DONE Cargo deps
- [X] Added =nix-bindings-store=, =nix-bindings-expr=, =nix-bindings-flake=
  (git) to =tools/nct/Cargo.toml=. =util=/=-sys= come transitively.
  Regenerated =Cargo.lock=.

*** DONE Build wiring in =modules/nct.nix=
Replicated upstream =nciBuildConfig=, simplified via clangStdenv:
- [X] =makeRustPlatform { stdenv = clangStdenv; }= (avoids gcc bindgen shim).
- [X] =nativeBuildInputs = [ pkg-config ]=, =buildInputs = [ nix stdenv.cc ]=.
- [X] =env.LIBCLANG_PATH= + =BINDGEN_EXTRA_CLANG_ARGS=.
- [X] Dev shell: =clangStdenv.mkDerivation= with same env + extra
      =-I${glibc.dev}/include= (bindgen calls libclang directly).
- [X] =cargoLock.allowBuiltinFetchGit = true= (no =outputHashes= maintenance).

*** DONE Runtime: thread/GC + EvalState bootstrap
- [X] =tools/nct/src/nix_eval.rs=: =init()= + =gc_register_my_thread()= guard
      on a dedicated OS thread (=std::thread::scope=).
- [X] =EvalStateBuilder::new(store)?.flakes(&FlakeSettings::new()?)?.build()?=.
- [X] =Store::open(None, HashMap::new())?=.

*** DONE =machine eval-expr <name> <expr>= command
- [X] clap =EvalExpr { name, expr }=; global =-f/--flake= (default =.=).
- [X] Logic: =getFlake= → dotted attr path navigation → =call(lambda, value)=
      → =print_value=.
- [X] Refactored generic =eval_flake_apply(flake_root, attr_path, expr)=
      under the =machine eval-expr= convenience wrapper.

*** DONE Tests
- [X] Unit: arg parsing (3), bootstrap error path (1).
- [X] Integration (=#[ignored]=, fixture-based, no network):
      =tests/fixtures/min-flake/flake.nix= — attr navigation + lambda apply.
- [X] CI =.forgejo/workflows/tool-tests.yaml= =nct-tests= job runs both
      =cargo test= and =cargo test -- --ignored=.

*** DONE Verification
- [X] =nix build .#nct= clean; =./result/bin/nct --version=.
- [X] =cargo clippy --all-targets= warning-free.
- [X] =cargo test= (4 pass) + =cargo test -- --ignored= (2 pass).
- [X] Playground:
      =nct machine eval-expr llm-runner 'm: builtins.attrNames m.config.llama-models.configurations'=
      → =[ "gemma4"; "granite-4.1-30b"; ... ]= (2.5s).

*** TODO Docs / follow-ups
- [ ] =tools/nct/README.md= usage section.
- [ ] Generalize CLI: =nct eval-expr <attr-path> <expr>= (currently only
      =machine eval-expr <name>=).
- [ ] JSON output flag.
- [ ] Decide whether to vendor =outputHashes= for reproducibility/pinning
      instead of =allowBuiltinFetchGit=. (Trade-off: drift vs provenance.)

** Open questions [resolved]
1. Which Nix package for the =buildInputs=? → *pkgs.nix* (decided).
2. Should =eval-expr= default to =.#nixosConfigurations.<name>= or accept a
   generic attribute path? → added generic =eval_flake_apply(flake_root,
   attr_path, expr)= under the hood; the CLI still exposes the
   =machine eval-expr <name>= convenience. Generalizing the CLI flag is a
   follow-up.
3. Output format → Nix-style pretty print (lists/attrsets with =;=-terminated
   entries). JSON is a follow-up if needed.
4. Where to run GC-registered nix work under =#[tokio::main]=? → dedicated OS
   thread via =std::thread::scope=+spawn (avoids tokio blocking nuances).

** Resolved along the way
- FFI build wiring: use *clangStdenv* (=makeRustPlatform= with
  =stdenv = clangStdenv=) instead of upstream's gcc bindgen shim — sidesteps
  the =features.h= not-found problem entirely. Dev shell still needs
  =BINDGEN_EXTRA_CLANG_ARGS= extended with =-I${glibc.dev}/include= because
  bindgen calls libclang directly (bypassing the cc wrapper).
- Git deps: =cargoLock.allowBuiltinFetchGit = true= avoids maintaining 11
  =outputHashes= that drift on every upstream bump.

* Stage 3 — experiment: adopt nix-cargo-integration (nci)
:PROPERTIES:
:STATUS: DONE
:END:

** Motivation
Stage 2 hand-wired a =rustPlatform.buildRustPackage= derivation for =nct=
and replicated upstream =nix-bindings-rust='s =nciBuildConfig= by hand
(=nixBindingsBuild= attr in =modules/nct.nix=). That works, but:
- The build config (pkg-config / libclang / bindgen args / clangStdenv) is
  duplicated between the package derivation and the dev shell, and maintained
  by us instead of consumed from upstream.
- We lose nci's niceties: automatic per-profile packages (dev/release),
  =check= / =clippy= / =docs= outputs, the crate-aware dev shell, lockfile
  generation app, etc.
- =nix-bindings-rust= *natively targets nci*: it ships an =nciBuildConfig=
  deferred module + an =input-propagation-workaround= that auto-adds the
  correct Nix C libraries (=nix-expr-c=, =nix-store-c=, ...) based on which
  =nix-bindings-*= crates are *direct* deps. Using nci lets us consume that
  instead of reverse-engineering it.

** What nci is
- A [[https://github.com/hercules-ci/flake-parts][flake-parts]] module
  ([[https://github.com/90-008/nix-cargo-integration][90-008/nix-cargo-integration]], checked out at
  =../nix/nix-cargo-integration/=).
- Builds Cargo projects via [[https://github.com/nix-community/dream2nix][dream2nix]]
  (crane-backed), provides a dev shell, and exposes per-crate outputs:
  =config.nci.outputs."<crate>".{packages,devShell,check,clippy,docs}=.
- Config surface (see =../nix/nix-cargo-integration/src/{interface,modules}=):
  - =nci.projects."<name>".path= :: project/workspace root.
  - =nci.crates."<name>"= :: per-crate config (=export=, =profiles=,
    =runtimeLibs=, =drvConfig= / =depsDrvConfig=).
  - =drvConfig= / =depsDrvConfig= :: =attrsOf anything= merged into the
    dream2nix derivation. Shape = { mkDerivation = {...}; env = {...}; }=.
    Set =deps.stdenv = pkgs.clangStdenv= to override stdenv (see
    =examples/customize-stdenv=).
  - =nci.toolchainConfig= / =nci.toolchains.{mkBuild,mkShell}= :: toolchain
    (defaults to =pkgs.rustPlatform= toolchain; rust-overlay optional).

** How =nix-bindings-rust= integrates with nci (=the native support=)
From =../nix/nix-bindings-rust/flake.nix= (=config.nix-bindings-rust=):
- =nixPackage= (default =pkgs.nix=).
- =nciBuildConfig= :: a *deferred module* to import into your crate's
  =drvConfig= / =depsDrvConfig=. It sets:
  - =mkDerivation.buildInputs = [ pkgs.stdenv.cc ]= (stdbool.h).
  - =mkDerivation.nativeBuildInputs = [ pkgs.pkg-config ]=.
  - =mkDerivation.postConfigure= = gcc bindgen shim (only if gcc stdenv).
  - =env.LIBCLANG_PATH=, =env.BINDGEN_EXTRA_CLANG_ARGS = "-x c++ -std=c++2a"=.
- =input-propagation-workaround.nix= :: auto-detects direct =nix-bindings-*=
  deps from the dream lock and adds the matching =nix-*-c= libraries to
  =buildInputs=. Toggle via
  =nix-bindings-rust.inputPropagationWorkaround.enable=.

So with nci + the =nix-bindings-rust= flake-parts module imported, our entire
=nixBindingsBuild= block in =modules/nct.nix= collapses to ~3 lines.

** Consumption pattern in *this* repo (dendritic + flake-file)
This flake is =flake-parts=-based and modules auto-discovered via
=import-tree ./modules=. Inputs are declared per-module through
=flake-file.inputs.<name>= (see =modules/formatter.nix=, =modules/srvos.nix=,
=modules/stylix.nix=) and regenerated with =nix run .#write-flake=. A
flake-parts =flakeModule= input is consumed by =imports = [
inputs.<x>.flakeModule ];= at the top of the module that needs it (e.g.
=modules/formatter.nix= imports =inputs.treefmt-nix.flakeModule=).

=> To adopt nci we add a =flake-file.inputs.nci= (and
=nci.inputs.nixpkgs.follows = "nixpkgs"=) somewhere, then =imports =
[ inputs.nci.flakeModule ]= + nci config in =modules/nct.nix=. We also need
the =nix-bindings-rust= flake-parts *module* (=config.nix-bindings-rust=)
available, which means a *flake input* for =nix-bindings-rust= (not just the
Cargo git dep). Note: =nix-bindings-rust= itself =imports nci=, so its flake
input will transitively pull nci too — we can =follows= them to dedupe.

** Proposed wiring (modules/nct.nix rewrite)
#+begin_src nix
{ self, inputs, ... }:
let
  selfLib = self.lib.self;
in
{
  flake-file.inputs = {
    nci = {
      url = "github:90-008/nix-cargo-integration";
      inputs.nixpkgs.follows = "nixpkgs";
    };
    # For the nciBuildConfig + input-propagation-workaround flake-parts module.
    # (The Cargo crates themselves still come via the git dep in Cargo.toml.)
    nix-bindings-rust = {
      url = "github:nixops4/nix-bindings-rust";
      inputs.nixpkgs.follows = "nixpkgs";
    };
  };

  imports = [
    inputs.nci.flakeModule
    inputs.nix-bindings-rust.modules.flake.basic # -> perSystem.nix-bindings-rust
  ];

  perSystem = { pkgs, config, ... }: {
    nci.projects."nct".path = selfLib.dir' "tools/nct";
    nci.crates."nct" = {
      export = true;  # -> config.nci.outputs."nct".packages.<profile> as flake packages
      depsDrvConfig.deps.stdenv = pkgs.clangStdenv;
      drvConfig = {
        imports = [ config.nix-bindings-rust.nciBuildConfig ];
        deps.stdenv = pkgs.clangStdenv;
      };
    };
  };
}
#+end_src
Then re-export as =packages.nct= / devshell / overlay from
=config.nci.outputs."nct"=, replacing the hand-rolled =packageFn= and
=devShells.nct=.

** Open questions / risks — detailed analysis
:PROPERTIES:
:CUSTOM_ID: stage3-risks
:END:
All three core risks have been *traced through the upstream source* (dream2nix
@ rev =dd8a3eb=, nci source in =../nix/nix-cargo-integration/src/=). Findings:

*** RISK 1 — flake-source leakage via =nci.projects.<n>.path=  (LOW)
:PROPERTIES:
:VERDICT: likely fine, but must verify empirically
:END:
- *What we need:* =nct='s source enters the store as a path whose hash depends
  /only/ on =tools/nct='s contents, so editing unrelated repo files does not
  rebuild =nct= (the repo invariant documented in =modules/flake-packages.nix=
  and enforced by =packages.check-no-flake-source-leak=).
- *How Stage 2 achieves it:* =selfLib.dir' "tools/nct"= evaluates to
  =builtins.path { path = self.outPath + "/tools/nct"; name = ...; }= — an
  *independent* store entry, content-addressed by that subtree.
- *How nci consumes path:* in =src/implementation.nix=,
  =evalCrate= sets =mkDerivation.src = mkForce project.path=, and
  =getProjectCrates.nix= does ="${path}/Cargo.toml"= (string coercion). A
  =builtins.path= coerces to a store path string just fine, and the
  =builtins.path= call (not =self= interpolation) is what governs the hash.
- *The subtlety / the actual risk:* dream2nix's =rust-cargo-lock= translator
  walks the source tree via =prepareSourceTree { source = cfg.source; }= and
  parses *all* =Cargo.toml= + =Cargo.lock= it finds. If =prepareSourceTree=
  materializes the whole =src= path verbatim (it does — it's an opaque tree
  copy), then because =selfLib.dir'="tools/nct"= already narrows to that
  subtree, only =tools/nct= contents matter. /If instead we passed =self= or
  a ="${self}/tools/nct"= string/, leakage would return. So the rule is:
  /keep passing the =builtins.path= (dir'), never a bare-string =self= path./
- *Failure mode if wrong:* =nix build .#nct= rebuilds on every unrelated edit;
  =check-no-flake-source-leak= (already in =devShells.default=) would catch
  it on the nixos-system closure, but =nct= itself is a dev tool not in the
  closure, so we must add a targeted check (see Probe #1).
- *Residual unknown:* whether dream2nix's =getRootSource= / =prepareSourceTree=
  pulls in anything beyond =src= (e.g. parent dirs for workspace resolution).
  =tools/nct= is a single-crate, not a workspace, so workspace-member globbing
  in =getProjectCrates.nix= is a no-op — low risk.

*** RISK 2 — transitive-input weight (dream2nix + crane + rust-overlay?)  (LOW–MED)
:PROPERTIES:
:VERDICT: acceptable; purely a cost, not correctness
:END:
- *New transitive inputs:* =nci= itself pulls =dream2nix=, =crane= (pinned
  =v0.19.0= / =v0.21.1= depending on nci rev), =rust-overlay=, =mk-naked-shell=.
  Note crane is a =flake: false= tarball (see =nci/flake.nix=), so it's a single
  fetch, not a live input. dream2nix is the heavy one (large repo).
- *Plus =nix-bindings-rust= as a flake input* (for the =nciBuildConfig= module)
  pulls its own =nix= + =nci= — but with =inputs.nixpkgs.follows= and nci
  deduped, the marginal cost is just one more lock node.
- *Impact:* larger =flake.lock= (cosmetic), slightly slower *first*
  =nix flake metadata= / =nix develop= (more nodes to fetch). No effect on
  =nix build= wall-time after inputs are cached, and /no/ effect on the
  =nct= derivation itself.
- *Why it's not a blocker:* every other flake-parts consumer in this repo
  (=treefmt-nix=, =stylix=, =clan-core=, =arion=, =direnv-instant=) already
  carries comparable weight. nci is in the same class.
- *Decision:* accept. Optionally measure =nix flake archive --dry-run |
  wc -l= before/after to quantify.

*** RISK 3 — git deps under dream2nix's =rust-crane= backend  (was: biggest unknown → now LOW)
:PROPERTIES:
:VERDICT: resolved by source reading — should "just work" with NO outputHashes
:END:
This was flagged as the biggest unknown. Reading dream2nix's actual vendoring
path resolves it favorably:

- *Pipeline:* =nci= → =dream2nix= =rust-cargo-lock= (parses =Cargo.lock= into
  a =dreamLock=) → =rust-cargo-vendor= (materializes each dep) → =rust-crane=
  (=crane.buildPackage= with =cargoVendorDir= pointing at the vendored tree).
- *How a git dep is parsed:* =translate.nix → parseSourceImpl= recognizes
  =git+<url>?<type>=<value>#<sha>= and emits ={type="git"; value={url; sha;
  type?; value?;};}= into the dreamLock =sources=.
- *How a git dep is fetched:* =lib/internal/fetchers/git/default.nix=,
  =fetched = hash: ...=. The decisive branch:
  #+begin_src nix
  fetched = hash:
    if hash == null
    then if ! refAndRev ? rev
         then throw "Cannot fetch git repo without integrity..."
         else builtins.fetchGit (refAndRev // { inherit url submodules;
               shallow = true; allRefs = ! hasGitRef; })
    else fetchgit (refAndRev // { inherit url; fetchSubmodules = submodules;
               sha256 = hash; });
  #+end_src
  i.e. when there is /no/ hash (the common case for git deps, which carry no
  =sha256= metadata in =Cargo.lock='s =[metadata]= table — only crates.io
  crates get checksums), dream2nix falls back to =builtins.fetchGit= with
  =shallow = true=. This is *precisely* the same primitive our current
  =cargoLock.allowBuiltinFetchGit = true= uses; dream2nix just invokes it
  itself during source materialization rather than letting cargo/nixpkgs do
  it during vendor.
- *Consequence:* the =nix-bindings-*= git deps should resolve with /zero/
  =outputHashes= maintenance — same trust model (content of the git rev) as
  today. The =sha= from =Cargo.lock= pins the rev; =builtins.fetchGit=
  verifies the tree is fetched at that rev.
- *Caveats that could still bite:*
  1. dream2nix rewrites =.cargo/config.toml= (via =writeGitVendorEntries=) to
     point git sources at the vendored dir. If =nix-bindings-rust='s crates
     have *path* deps among themselves (they're a workspace!),
     =replaceRelativePathsWithAbsolute= must rewrite them. The upstream repo
     dogfoods nci on itself, so this path /is/ exercised — but our
     consumption is as a *consuming crate*, not the workspace root, so the
     path-rewrite logic runs against the /fetched/ nix-bindings tree, which is
     intact. Low risk, but Probe #2 confirms.
  2. =builtins.fetchGit= during evaluation = pure eval incompatibility. This
     repo is /not/ evaluated in pure-eval mode for builds, so fine; but if we
     ever run =nix flake check --pure-eval=, git deps without hashes fail.
     Stage 2 has the identical limitation today.
  3. dream2nix's =prepareSourceTree= / =translate= run during /evaluation/
     (IFD-free; they just read files), but =fetchDreamLockSources= (the
     =builtins.fetchGit= calls) also run at eval time. That means *every*
     =nix eval= touching =packages.nct= fetches the git deps unless cached in
     the nix store. Same as today's =allowBuiltinFetchGit=. No regression.
- *Net:* Risk #3 is downgraded from "biggest unknown" to "verify with a real
  build". The mechanism is sound and matches our existing trust model.

*** RISK 4 — dev-shell parity  (LOW)
:PROPERTIES:
:VERDICT: mechanical, solvable via overrideAttrs
:END:
- nci's dev shell (=modules/dream2nix/rust-crane/devshell.nix=) is
  =mkShell.override { stdenv = mainDrv.out.stdenv; }= with the union of
  =buildInputs=/=nativeBuildInputs= from deps + main drv, plus =cargo=.
- It does /not/ add =rust-analyzer= / =clippy= / =rustfmt= by default.
- *Fix:* =devShells.nct =
  config.nci.outputs."nct".devShell.overrideAttrs (old: {
    packages = (old.packages or []) ++ [ rust-analyzer clippy rustfmt ];
  });= — exactly the =customize-devshell= example pattern.
- *BINDGEN_EXTRA_CLANG_ARGS / LIBCLANG_PATH:* these come from the =nciBuildConfig=
  =env=, which dream2nix propagates into the dev shell (the devshell module
  copies =mkDerivation= env, filtering an illegal-vars blocklist — and
  =LIBCLANG_PATH=/=BINDGEN_EXTRA_CLANG_ARGS= are /not/ in that blocklist).
  So the FFI env should land in the shell automatically. Probe to confirm.
- *Default-devshell registration* (=modules/devshell.nix=): =self'.packages.nct=
  is unaffected (still a package output). The dedicated =devShells.nct=
  just changes its construction.

*** RISK 5 — CI wiring  (LOW)
:PROPERTIES:
:VERDICT: keep current steps; optionally enrich later
:END:
- Current =nct-tests= job runs =cargo {clippy,test,test --ignored}= inside
  =.#devShells.x86_64-linux.nct= + =nix build .#nct=. All of that still works
  after the rewrite (the shell + package still exist, just built differently).
- /Optional/ improvement: add CI steps building
  =config.nci.outputs."nct".{check,clippy}= as flake checks for cached,
  reproducible test runs. Not required; defer.

*** RISK 6 & 7 — minor (numtide shell, output naming)  (TRIVIAL)
- Numtide =devshell=: not used anywhere in this repo; nci's =mkShell=-based
  shell is the right choice. No action.
- Output naming: =config.nci.outputs."nct".packages.{dev,release}=; we
  re-export =packages.nct = outputs.packages.release= explicitly. Trivial.

** Experiment plan (confirmation probes — risks now LOW after source review)
All three core risks are resolved by reading the upstream source. The probes
below are *confirmations*, not open investigations — do them on a throwaway
branch before the real rewrite, but expect them to pass.

1. *Spike on a throwaway branch.* Add =nci= + =nix-bindings-rust= flake inputs
   (via =flake-file.inputs= with =nixpkgs.follows=), =git add --intent-to-add=
   any new file, =nix run .#write-flake=.
2. *Probe #1 (path leakage — confirms RISK 1):* in a scratch module, set
   =nci.projects."nct-probe".path = selfLib.dir' "tools/nct"= and build
   =config.nci.outputs."nct-probe".packages.dev=. Then =nix path-info --derivation=
   on the result, and grep the .drv's inputSrcs / =nix derivation show= JSON for
   any store path whose name does *not* start with the isolated =tools__nct=
   hash. Expected: only the isolated crate dir. /Bonus:/ run
   =packages.check-no-flake-source-leak= against =packages.nct= once wired.
3. *Probe #2 (git deps — confirms RISK 3):* build =packages.nct= via nci
   (=config.nci.outputs."nct".packages.release=). Expected: success with no
   =outputHashes= and no manual vendoring — dream2nix calls
   =builtins.fetchGit { shallow = true; }= for the =nix-bindings-*= deps
   (verified in =fetchers/git/default.nix=). If it /does/ fail, the
   =writeGitVendorEntries= / =replaceRelativePathsWithAbsolute= step is the
   likely culprit (workspace path deps); inspect the failing phase.
4. *Probe #3 (nciBuildConfig import — confirms the core payoff):* confirm
   =config.nix-bindings-rust.nciBuildConfig= imports cleanly into the crate
   =drvConfig= and that =nix derivation show= on the resulting =nct= .drv has
   =nix-expr-c= / =nix-store-c= / =nix-flake-c= in =buildInputs= (added by
   the =input-propagation-workaround=) and =LIBCLANG_PATH= /
   =BINDGEN_EXTRA_CLANG_ARGS= in =env=. This is the whole point of the
   migration — verifies we can delete =nixBindingsBuild= from
   =modules/nct.nix=.
5. Only if all probes pass → rewrite =modules/nct.nix= to the proposed wiring,
   re-export =packages.nct= + =devShells.nct= + overlay from nci outputs,
   update CI, regenerate flake, and update this section to DONE with
   verification.

** Decision gate (updated — lower-risk posture)
- *Expected outcome:* all probes pass → adopt nci (Stage 3 DONE). Win:
  upstream-maintained nix-bindings build config (=nciBuildConfig= +
  propagation workaround) replaces our hand-maintained =nixBindingsBuild=,
  plus nci gives per-profile packages + =check=/=clippy=/=docs= outputs for
  free.
- *If Probe #1 leaks (unlikely):* keep Stage 2's =builtins.path= discipline
  but file an nci issue; fall back to manual =buildRustPackage=.
- *If Probe #3 fails (unlikely — it's upstream's own dogfooded path):* keep
  Stage 2 manual build; nci provides only marginal value without it.
- *If Probe #2 fails:* two fallbacks — (a) pin the nix-bindings rev and add a
  local =.cargo/config.toml= path override pointing at
  =../nix/nix-bindings-rust= so dream2nix sees a *path* dep (which it handles
  via =replaceRelativePathsWithAbsolute=) instead of a git dep; (b) keep
  Stage 2 manual =buildRustPackage=. Git-dep handling is the one place
  dream2nix has historically been fragile, so don't over-invest if it fights.

** Status [DONE]
- [X] Spike branch created (=nct-nci-spike=).
- [X] Probe #1 (path leakage) — PASS. =nix derivation show .#nct= →
      =env.src = /nix/store/...-tools__nct= (isolated =builtins.path=, not
      =self=).
- [X] Probe #2 (git deps under crane) — PASS. =nix build .#nct= compiles
      the =nix-bindings-*= git deps with ZERO =outputHashes=; dream2nix calls
      =builtins.fetchGit { shallow = true; }= exactly as analyzed.
- [X] Probe #3 (nciBuildConfig import) — PASS. =config.nix-bindings-rust.nciBuildConfig=
      imports into both =drvConfig= and =depsDrvConfig=; the FFI crates
      (=nix-bindings-*-sys=) compile via the propagated =nix-*-c= libraries.
- [X] Rewrite =modules/nct.nix=.
- [X] Update CI =nct-tests= job.
- [X] =nix run .#write-flake= + verify =nix build .#nct= / dev shell / tests.

** Decisions / what changed (vs the plan)
- *nciBuildConfig goes in BOTH =drvConfig= AND =depsDrvConfig=*, not just
  =drvConfig= as the plan proposed. The plan missed that the
  =nix-bindings-*-sys= crates (which run =build.rs= with pkg-config + bindgen)
  are compiled during the *deps* build. nix-bindings-rust's own =nci.nix=
  hints at this ("Downstream projects import this into depsDrvConfig
  instead"). Without it, deps build fails with "pkg-config command could not
  be found".
- *Use nci's NATIVE clippy output, not =pkgs.clippy= in the dev shell.* The
  plan proposed adding =pkgs.clippy=/=rustfmt= to the dev shell; that breaks
  because nci's toolchain (rust-overlay, e.g. 1.97.1) and nixpkgs' clippy
  (e.g. 1.95.0) are different rustc versions → =E0514 "incompatible
  version"=. Instead: expose =nciOut.clippy= / =nciOut.check= as flake
  =checks= (built with the correct toolchain), and in the dev shell add ONLY
  =rust-analyzer= (not bundled in the rust toolchain) — =cargo-clippy= /
  =clippy-driver= / =rustfmt= come from the toolchain already.
- *Dropped the =flake.overlays.nct=* (it had no consumers: not in
  =modules/nix.nix= =defaultOverlays=, nothing uses =pkgs.nct=). =nct= is only
  consumed as =self'.packages.nct= in =modules/devshell.nix=. Re-add if a
  consumer appears.
- *Dev-shell BINDGEN_EXTRA_CLANG_ARGS still needs =-I${glibc.dev}/include=*
  appended (same Stage 2 fix) — =nciBuildConfig.env= only sets
  ="-x c++ -std=c++2a"=, and in the dev shell bindgen calls libclang
  directly (bypassing the cc wrapper), so it can't find =features.h=.
- *Ignored integration tests stay shell-based in CI.* nci's native =check=
  runs =cargo test= but won't run =#[ignore]d= tests (which need a relocated
  nix store + network). CI keeps a =cargo test -- --ignored= step in the dev
  shell for those.

** Verification [DONE]
- [X] =nix build .#nct= clean; =nct --version= = =nct 0.1.0=.
- [X] =nix build .#checks.x86_64-linux.nct-clippy= — warning-free.
- [X] =nix build .#checks.x86_64-linux.nct-test= — 4 passed, 2 ignored.
- [X] Dev shell: =cargo test= (4 pass) + =cargo test -- --ignored= (2 pass)
      + =cargo clippy -- -D warnings= clean (rustc & clippy both 1.97.1).
- [X] Playground:
      =nct machine eval-expr llm-runner 'm: builtins.attrNames m.config.llama-models.configurations'=
      → 7 configs.
- [X] Default devshell still has =nct= on PATH (=self'.packages.nct=).
- [X] =nix fmt= covers =.rs= files (rustfmt program).
- [X] No flake-source leak: =env.src= is the isolated =tools__nct= path.

* Stage 4 — =machine provision-pve= (cloud-image + clan age key via cloud-init)

** Goal

A single =nct machine provision-pve <name> -p <proxmox-host>= command that
provisions a *cloud-image-based* VM on Proxmox, with the clan age key for the
machine injected via cloud-init so the custom Stage-1 =provision-clan-key=
initrd unit can bootstrap clan/sops secrets on first boot.

This is the =ncf machine provision-vm= flow, but:
- The disk is a prebuilt qcow2 =cloudImage= (=make-disk-image.nix=, see
  =modules/machines/xray-exit.nix=) instead of disko =diskoImages=.
- Secrets are *clan*-managed: the machine's age private key
  (=sops/secrets/<machine>-age.key=) is decrypted via clan's =SecretStore=
  and planted into the NoCloud =user-data=, not the legacy SSH-host-key-only
  path.

** What =ncf= already does (reference for the =nct= port)

=ncf='s Proxmox interaction is entirely over SSH (no Proxmox API token):
- =ncf/proxmox_api.py= =ProxmoxClient= uses =proxmoxer= with the
  =ssh_paramiko= backend (auth = root SSH keys from =~/.ssh/=). It clears
  =SSH_AUTH_SOCK=/=SSH_AGENT_PID= to dodge paramiko agent bugs.
- =qm= commands are issued by *shelling out to plain =ssh root@<host>=*
  with =shlex.join(remote_cmd)= — NOT via the proxmoxer API. See
  =provision_vm.py ssh_command()= and =provision_vm_anywhere.py=. This is the
  key reuse insight: **we don't need a proxmox client library at all** —
  =ssh root@<host> qm ...= is the whole API surface we touch.
- =upload_snippet()= opens a raw paramiko/SFTP session to write
  =/var/lib/vz/snippets/<file>=. The =nct= equivalent can do the same over
  =ssh ... cat > /var/lib/vz/snippets/<file>= (or =scp=).
- =upload_iso()= streams a file via =pv | ssh root@<host> 'cat > ...'=.

So =nct='s runtime deps for this are minimal: =ssh=, =shlex=-equivalent
shell quoting (we already shell out to =ssh= via =tokio::process::Command=).
No Rust =proxmoxer= equivalent exists/needed. (Optionally =rsync= for the
  qcow2 copy, or =ssh cat >= like ncf's ISO path — ncf uses =rsync -avP= for
  the raw disk in =provision_vm.py= Step 8; we'll mirror that.)

*** The full =provision-vm= flow (=provision_vm.py run())
1. Query NixOS config for metadata: =config.networking.hostName=,
   =config.nixos-config.qemu-guest.proxmox= (memory/cores/bios/disks/
   cloudInit/tpm2/efidisk/...), =config.inventory.hostIpAllocation.<net>=,
   =config.inventory.networks.<net>= (prefix/gateway/vlan/dns).
2. Check VM existence by hostname (=qm list= via the API → for us, =ssh qm
   list= / parse, or =ssh qm config <vmid>= on a guessed id).
3. Build the disk image (=ncf build vm= → =diskoImages=). **We replace
   this with =system.build.cloudImage=**
   (=nixosConfigurations.<name>.config.system.build.cloudImage=, qcow2).
4. Generate cloud-init =user-data= (YAML, =#cloud-config= header) and upload
   as a snippet; wire via =qm create ... --cicustom user=<storage>:snippets/<file>=.
   **This is where we inject the clan age key.**
5. =qm create= with the static =--ipconfig0=, =--net0= (bridge/mac/vlan),
   =--serial0 socket --vga serial0=, =--agent 1=, memory/cores/bios/machine,
   =--scsihw virtio-scsi-single=, optional =--onboot= / =--balloon= / =--shares=.
6. UEFI: =qm set <vmid> --efidisk0 <storage>:1,efitype=4m,pre-enrolled-keys=0|=1=.
7. TPM2 (if enabled): =qm set <vmid> --tpmstate0 <storage>:1,version=v2.0=.
8. Import disk: =rsync= the image to =/tmp/<machine>-disk.qcow2= on the host,
   then =qm set <vmid> --scsi0 <storage>:0,import-from=/tmp/...=, then
   =qm set <vmid> --boot order=scsi0=, then =rm= the temp file.
9. Optionally =start= the VM.

*** The clan age-key injection (the important bit)

The target module =modules/provision-clan-key.nix= defines an initrd systemd
unit =provision-clan-key.service= that, during Stage 1 (after =sysroot.mount=,
before =initrd.target=):
- mounts the NoCloud seed =/dev/disk/by-label/cidata= (iso9660 or vfat),
- greps =^AGE-SECRET-KEY-1= out of the seed's =user-data= file,
- writes it to =/sysroot<secretLocation>/key.txt= (chmod 600),
where =secretLocation = clan.core.vars.age.secretLocation or
"/etc/secret-vars"= (default =/var/lib/sops-nix= per
=clanCore/vars/secret/sops/default.nix=, but =modules/impermanence.nix=
overrides to =/persist/var/lib/sops-nix=).

So the =nct provision-pve= command must:
1. Decrypt the machine's age private key. Source of truth: the sops secret
   =sops/secrets/<machine>-age.key= (a sops-encrypted JSON blob with
   =data= holding the key). Clan decrypts it via =SecretStore.populate_dir=
   (phases =users= → writes =key.txt=). Two implementation options for =nct=:
   - **(A) Shell out to =clan secrets get=** (or clan_lib) — mirrors how the
     value travels at runtime, no reimplementation. This is the =ncf= way
     (=gather_secrets_for_clan_machine= calls clan_lib's =SecretStore=).
     Simplest, stays correct as clan evolves.
   - **(B) Shell out to =sops -d sops/secrets/<machine>-age.key=** and parse
     the JSON =.data= field. More fragile (couples to sops JSON layout);
     avoid unless clan isn't on PATH.
   → Prefer (A): =clan secrets get <machine>-age.key= prints the raw key to
     stdout (it's what =ncf= / clan_lib effectively do under the hood).
2. Embed it in the cloud-init =user-data= as a =write_files= entry, e.g.:
   #+begin_src yaml
   #cloud-config
   write_files:
     - path: /user-data   # not really — see below
   #+end_src
   …except =provision-clan-key= reads the *seed's user-data file directly*
   (the whole NoCloud =user-data=), not a =write_files= target. So the key
   just needs to be a plaintext line in the =user-data= the seed drive
   serves. Simplest: append the bare =AGE-SECRET-KEY-1...= line to the
   =user-data= YAML (cloud-init ignores trailing junk it can't parse as
   cloud-config keys; the grep anchor =^AGE-SECRET-KEY-1= is robust). Or wrap
   it in a =write_files= entry that's also valid YAML — either works because
   the initrd unit only greps. **Decision: append the bare key line at the
   end of user-data** (matches what =provision-clan-key= expects and is
   minimally invasive).
3. Upload =user-data= as a Proxmox snippet and reference via =--cicustom
   user=<storage>:snippets/<machine>-cloud-init.yaml=.

Note: Proxmox's CloudInit drive (=qm set <vmid> --ide0 <storage>:cloudinit=)
provides the =cidata=-labelled NoCloud seed that =provision-clan-key=
mounts. =--cicustom user=...= makes Proxmox bake our =user-data= into that
seed drive. So the wiring is: snippet → =cicustom= → CloudInit drive →
NoCloud seed → initrd grep → =key.txt=. All already designed for this.

** The disk image

=modules/machines/xray-exit.nix= defines:
#+begin_src nix
system.build.cloudImage = import "${pkgs.path}/nixos/lib/make-disk-image.nix" {
  inherit lib pkgs config;
  format = "qcow2";
  partitionTableType = "efi";
  additionalSpace = "1024M";
};
#+end_src
Build + locate:
#+begin_src shell
nix build --no-link --print-out-paths '.#nixosConfigurations.xray-exit.config.system.build.cloudImage'
# → /nix/store/...-nixos-disk-image/nixos.qcow2
#+end_src
So =nct= builds that attr and reads =<out>/nixos.qcow2=. (Unlike disko's
=result/main.raw=, the cloud image is a single qcow2.) Import path differs
slightly from ncf: =import-from=/tmp/<machine>.qcow2= (qcow2, not raw) —
Proxmox's =qm set ... --scsi0 <storage>:0,import-from=...= handles qcow2
fine.

** Proposed =nct= implementation

*** CLI
#+begin_src
nct machine provision-pve <name> \
  --proxmox-host <host>        # -p, required
  [--network home|guest]       # inventory network (REQUIRED: picks the IP,
                               #   gateway, prefix, dns, vlan for both the
                               #   qm NIC and cloud-init networking)
  [--bridge vmbr0]             # physical bridge on the proxmox side
  [--snippet-storage local]    # dir-type storage for cloud-init snippets
                               #   (default: local; resolved via
                               #   `pvesm path <storage>:snippets/<file>`)
  [--disk-storage local-zfs]   # disk + cloud-init drive storage
  [--start]                    # start after provision
  [--dry-run]
#+end_src
=--network= selects *which* inventory network (home / guest / ...) the VM is
wired into — xray-exit lives on =guest= (vlan 4, 192.168.3.0/24), so
provision would be =... --network guest=. Everything (IP, gateway, prefix,
DNS, VLAN tag, MAC) is read from =config.inventory.{hostIpAllocation,networks}.<network>=.

=--snippet-storage= defaults to =local= (the conventional snippets
storage) per the valak probe: snippets only resolve on =dir=-type storages
(=pvesm path local-zfs:snippets/x= fails with "unable to parse zfs volume
name"; only =dir: local= works, mapping to =/var/lib/vz/snippets/=). We
=pvesm path= it to find the write dir and fail loudly if it doesn't
resolve. (To *enable* snippets on =local=, add it to the content list:
=pvesm set local --content snippets,iso,backup,vztmpl= — already done on
valak.)

*** Config introspection via nix-bindings (NOT =nix eval --json=)

**Decision: use the in-process EvalState (already wired in Stage 2), not
=nix eval= subprocesses, for all config introspection.** Rationale:
- A single =builtins.getFlake= + navigation through one long-lived
  =EvalState= means the full =nixosConfigurations.<name>= module eval is
  computed **once** and memoised in the EvalState's value graph. Every
  subsequent =require_attrs_select= / =require_string= / =require_int=
  reuses the already-forced thunks → near-free. ncf spawns a fresh =nix eval=
  per attribute (=NixRunner.run_eval= × ~6), each re-evaluating the whole
  module system from scratch (or hitting the eval cache, but still
  process-spawn overhead + re-navigation).
- We already pay for the FFI build (Stage 2/3); reusing it for introspection
  is the actual ROI beyond the =eval-expr= playground.
- The API surface we need is tiny and already proven by =eval_flake_apply=:
  =eval_from_string= (getFlake), =require_attrs_select{,_opt}= (navigate),
  =require_string= / =require_int= / =require_bool= / =require_list_strict=
  (extract typed leaves). No serde structs for the whole qemu-guest schema —
  we navigate attrset-by-attrset and build a small Rust =VmConfig= struct by
  hand. This is *more* robust than a big serde derive (schema drift only
  breaks the one field being read, not the whole parse).

Proposed =nix_eval.rs= additions (a long-lived =NixFlake= handle):
#+begin_src rust
pub struct NixFlake {
    es: EvalState,           // GC-registered-thread-owned
    flake_value: Value,      // builtins.getFlake "<root>"
    // thread handle / scope guard so all calls land on the GC thread
}
impl NixFlake {
    pub fn open(flake_root: &str) -> Result<Self>;
    /// Navigate dotted attr path under the flake root, return the Value.
    pub fn attr(&mut self, path: &str) -> Result<Value>;
    /// Convenience: attr + require_string.
    pub fn attr_str(&mut self, path: &str) -> Result<String>;
    pub fn attr_int(&mut self, path: &str) -> Result<i64>;
    pub fn attr_bool(&mut self, path: &str) -> Result<bool>;
    /// Load a whole nixosConfigurations.<name> once; subsequent
    /// config.* reads reuse the forced value.
    pub fn machine_config(&mut self, machine: &str) -> Result<MachineConfig>;
}
#+end_src
The =EvalState= must live on the GC-registered thread (Stage 2 invariant),
so =NixFlake= either owns a dedicated thread + channel, or exposes a
=with_es(|es| ...)= closure API. Simplest: keep the =std::thread::scope=
pattern but hold it open for the process lifetime via a ='static= worker
thread (spawn once in =main=, send closures over a channel). Defer the
exact ergonomics; the point is *one EvalState, many reads*.

Query plan (all off one =machine_config= force):
- =config.networking.hostName=
- =config.networking.hostId= (→ vsock CID)
- =config.nixos-config.qemu-guest.proxmox= → ={memory, balloon?, cores,
  sockets?, bios, machine, scsihw?, onboot?, agent?, shares?, description?,
  disks[], network{bridge,model,firewall}, cloudInit{enable,storage},
  tpm2{enable,storage?,version?}, efidisk{storage,efitype,secureBoot},
  pci-passthrough{}}=
- =config.inventory.hostIpAllocation.<network>.primary= → ={address, mac?}=
- =config.inventory.networks.<network>= → ={prefix, gateway?, dns?[],
  vlan?, network?, searchdomain?}=
- =config.users.users.root.openssh.authorizedKeys.keys= → =[string]

*** Cloud-init networking from the chosen network

Since cloud-image VMs have =staticNetwork = false= (systemd-networkd not
wired), cloud-init is the sole network configurator. We generate **two**
cloud-init inputs in sync:
1. A NoCloud =network-config= (v2) snippet, wired via =--cicustom
   network=<snippet-storage>:snippets/<machine>-ci-network.yaml=. This is
   what the guest actually consumes (addresses/routes/nameservers/search).
2. The matching Proxmox-native =--ipconfig0 ip=X/prefix,gw=Y= + =--nameserver=
   for the same values. =--ipconfig0= is reflected in =qm config= / the web
   UI / =qm cloudinit dump=, while =cicustom network= is what lands in the
   NoCloud seed (probe-confirmed: Proxmox bakes =cicustom= files *verbatim*
   into the seed ISO; =ipconfig0= generates Proxmox's *own* network-config
   which =cicustom network= overrides). **Setting both, in sync** keeps
   =qm config= self-documenting for operators while the actual seed comes
   from our v2 snippet (full DNS + search domain, which =ipconfig0='s
   =gw=/nameserver= can't fully express). Belt + suspenders: if they ever
diverge it's a bug, but they won't — both derive from the one inventory
   network the operator passed via =--network=.

If the net has a =vlan=, tag it on the =qm --net0 ...tag=<vlan>= side (host
bridges onto the right VLAN; guest sees untagged eth0).

So the snippet set per VM becomes:
- =<machine>-ci-user.yaml= → =#cloud-config= + authorized_keys + the age
  key under a dedicated =clan-machine-key:= top-level key (see below).
- =<machine>-ci-network.yaml= → Network Config v2 from the chosen inventory
  network.
- =--cicustom user=<snippet-storage>:snippets/<machine>-ci-user.yaml,network=<snippet-storage>:snippets/<machine>-ci-network.yaml=
  + =--ipconfig0= / =--nameserver= mirroring the network values.

*** Age-key injection: keep user-data valid YAML

The age key goes into =user-data= under a dedicated top-level key, not a
bare trailing line:
#+begin_src yaml
#cloud-config
hostname: xray-exit
ssh_authorized_keys: ["ssh-ed25519 AAA..."]
clan-machine-key: AGE-SECRET-KEY-1...base64...
#+end_src
Why:
- The whole document stays valid YAML (cloud-init tolerates unknown keys —
  each =cc_= module only reads the keys it cares for; an extra top-level
  string key is ignored by the cloud-config pipeline).
- The initrd extractor is a *targeted* grep:
  =grep -m1 '^clan-machine-key:' <user-data>=, then strip the prefix, then
  =case "$key" in AGE-SECRET-KEY-1*) ...= . More precise than scanning for
  =^AGE-SECRET-KEY-1= and self-validating (rejects a malformed value).
=modules/provision-clan-key.nix= is already updated to this scheme
(=clan-machine-key:= grep + =AGE-SECRET-KEY-1= case guard; escaped the bash
=${...}= as =''${...}= in the Nix =''=-string to dodge Nix interpolation).

*** Modules / code layout (=tools/nct/src/=)
- =main.rs= :: add =ProvisionPve= variant to =MachineCommand=, clap fields
  as above (=--network= required).
- =nix_eval.rs= :: promote to a long-lived =NixFlake= handle (one EvalState,
  GC-registered worker thread, closures-over-channel). Add typed accessors
  (=attr= / =attr_str= / =attr_int= / =attr_bool=) and a =MachineConfig=
  loader that forces the whole machine value once.
- =proxmox.rs= (new) :: =ssh_qm(host, args)= → runs =ssh root@<host> qm
  <args>= via =tokio::process=, returns stdout/stderr. =upload_snippet=
  via =ssh ... cat > /var/lib/vz/snippets/<name>= (stdin pipe). =rsync_image=
  via =rsync -avP <local> root@<host>:<remote>=.
- =cloud_init.rs= (new) :: build (a) the =user-data= string:
  =#cloud-config= + hostname + ssh_authorized_keys + trailing bare
  =AGE-SECRET-KEY-1= line; (b) the =network-config= v2 from the chosen
  inventory network (addresses/routes/nameservers/search).
- =provision_pve.rs= (new) :: orchestrate the 9-step flow, mirroring
  =ncf/commands/provision_vm.py run()=, but:
  - build =system.build.cloudImage= (qcow2) not =diskoImages=,
  - read config via the =NixFlake= handle (one eval),
  - generate both =user-data= and =network-config= snippets,
  - =qm import-from= the qcow2.

*** Secret retrieval
- =clan secrets get <machine>-age.key= via =Command=; capture stdout,
  strip trailing newline. Fail loudly if missing (machine not clan-managed
  / not initialized).
- Optionally =--fake-secrets= for dry-run (ncf has it); defer.

** Tasks
- [X] =NixFlake= long-lived handle (=nix_flake.rs=: dedicated GC-registered
      worker thread + mpsc channel of closures; flake eval cached for the
      handle's lifetime).
- [X] =MachineConfig= typed loader (=config.rs=: one Nix lambda returning
      =builtins.toJSON= of the needed attrs; parsed into typed Rust structs).
- [X] =ProvisionPve= clap variant (=--network= required, =--bridge=,
      =--snippet-storage= (default =local=), =--disk-storage=, =--start=,
      =--dry-run=) + wiring in =main.rs=.
- [X] =proxmox.rs=: =qm=, =run_remote=, =vmid_for_name= (=qm list= grep),
      =next_vmid=, =snippet_path= (=pvesm path=), =write_file= (ssh stdin),
      =rsync_to=.
- [X] =cloud_init.rs=: =user_data= (hostname + authorized_keys +
      =clan-machine-key:= top-level key) and =network_config= (Netplan v2).
- [X] =provision_pve.rs=: full orchestration (load config → age key via
      =clan secrets get= → existence check → build cloudImage → upload
      snippets → =qm create= with cicustom user+network + ipconfig0 in sync →
      EFI/TPM → qcow2 import → boot order → optional start).
- [X] Dry-run mode (prints all qm commands + snippet contents, skips network).
- [ ] Runtime deps in =modules/nct.nix=: =ssh=/=rsync=/=nix= assumed from
      devshell/PATH (matches ncf); =clan= required for age-key decrypt
      (=inputs'.clan-core.packages.clan-cli-full=, devshell-only for now).
- [ ] End-to-end test against =xray-exit= on a real Proxmox host
      (=--network guest=, vlan 4). Dry-run validated; live run pending.
- [ ] Docs in =tools/nct/README.md=.

** Open questions
1. ~~=clan= on PATH~~ → resolved: =inputs'.clan-core.packages.clan-cli-full=
   (determinate-patched); devshell-only for now. (See tasks.)
2. ~~Proxmox snippets storage~~ → resolved by probe on =valak=:
   snippets only resolve on =dir=-type storages. =pvesm path
   local-zfs:snippets/foo= fails ("unable to parse zfs volume name");
   only =dir: local= works → =/var/lib/vz/snippets/foo=. May need
   =pvesm set local --content snippets,...= to enable the content type
   (done on valak). → =--snippet-storage= is explicit, no defaulting; we
   =pvesm path= it to find the write dir.
3. ~~qcow2 import on zfspool~~ → resolved by probe: =qm set <vmid> --scsi0
   local-zfs:0,import-from=/tmp/x.qcow2= transfers + converts the qcow2 to
   a zvol cleanly ("transferred 1.0 MiB ... successfully created disk").
   No =qemu-img convert= needed on our side.
4. ~~cicustom user+network actually reaching the guest~~ → resolved by probe:
   built a scratch VM with =--cicustom
   user=local:snippets/...yaml,network=local:snippets/...yaml= +
   =--ide0 local:cloudinit=, then extracted the generated cloud-init ISO
   (=qemu-img convert= the qcow2 cdrom → =mount -o loop=). Result:
   - =user-data= file contains our YAML **verbatim**, including a trailing
     bare =AGE-SECRET-KEY-1TEST...= line.
   - =network-config= file contains our Network Config **v2** verbatim.
   - =meta-data= / =vendor-data= populated by Proxmox. So the NoCloud seed
     (label =cidata=) is exactly what =provision-clan-key= mounts. ✓
   Note: =qm cloudinit dump <vmid> user|network= shows Proxmox's
   *generated* (merged-from-ipconfig/ciuser) config, NOT the cicustom
   snippets — so don't use =dump= to verify cicustom; extract the ISO.
5. ~~YAML validity of the age-key injection~~ → resolved: plant the key
   under a dedicated =clan-machine-key:= top-level key (valid YAML;
   cloud-init ignores unknown top-level keys). =provision-clan-key.nix=
   already updated: targeted =grep '^clan-machine-key:'= + =case=
   guard for =AGE-SECRET-KEY-1=, with the bash =''${...}= escaped in the
   Nix =''=-string. Module eval verified clean.
6. VM existence check without proxmoxer: =qm list= column format confirmed
   on valak — =VMID NAME STATUS MEM(MB) BOOTDISK(GB) PID=; =NAME= is the
   hostname. =ssh root@<host> qm list= + grep the NAME column (or parse all
   rows) works.
7. Threading for =NixFlake=: the BDW GC supports *many* registered threads
   (=GC_register_my_thread= per-thread, RAII guard in nix-bindings). The
   real constraint is that =EvalState= / =Value= hold raw pointers →
   =!Send=, so they're thread-/affine. **Decision: own the EvalState on
   one dedicated worker thread for the lifetime of =NixFlake=, send
   closures over a channel.** (Not =scope= per-command — we want the flake
   eval cached across the whole provision run's many reads, and a single
   worker thread is simpler than reasoning about =!Send= across scopes.)
   If we ever need parallel eval, register more threads — but one is fine
   here; introspection is sequential.
8. ~~Cloud-init networking~~ → resolved by probe + design: generate a
   NoCloud =network-config= v2 snippet (=cicustom network=) AND mirror
   the values into Proxmox-native =--ipconfig0= / =--nameserver= (in sync,
   both from the one =--network= inventory net). =cicustom network= is
   what the guest consumes (probe-confirmed verbatim passthrough);
   =ipconfig0= keeps =qm config= / the UI self-documenting. nixos
   cloud-init (=services.cloud-init.network.enable=) consumes the
   NoCloud network-config.
