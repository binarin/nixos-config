pve_at_home:
  hosts:
    raum:
    bael:
    valak:

pve:
  children:
    pve_at_home:

qdevices:
  hosts:
    qdevice-vm:

pbs:
  hosts:
    bael:
    pbs-hetzner:

debian_cts:
  hosts:
    paperless:
    docker:
    dnscontrol:
    nextcloud:
    unifi:

console_autologin:
  children:
    debian_cts:
    debian_vms:

manage_ssh_access:
  children:
    debian_cts:
    debian_vms:
    pve:
    pbs:

manage_root_ssh_access:
  children:
    pve:
    pbs:

all:
  children:
    pve:
    pbs:
  vars:
    ansible_user: root
